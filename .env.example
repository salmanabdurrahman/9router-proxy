# Secret authentication token for 9Router Edge Relay
# Must be at least 16 characters long. Generate using: openssl rand -hex 24
RELAY_SECRET=replace_with_a_secure_random_secret_at_least_16_chars

# Port for local development runner (optional, default: 8787)
PORT=8787
